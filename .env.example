# Copy this file to .env for local development
# On Railway: add these as environment variables in Settings → Variables

# Required: your admin panel password
ADMIN_PASSWORD=changeme

# Optional: defaults to 3000
PORT=3000
